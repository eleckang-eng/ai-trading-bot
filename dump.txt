e_badge = "🧪 테스트 (미전송)"
    elif curr_mock:
        mode_badge = "🟢 모의 계좌"
    else:
        mode_badge = "🔴 실전 투자"

    st.markdown(f"**현재 적용 모드**: `{curr_ex.upper()}` · `{current_sym(current_cfg)}` · **{mode_badge}**")
    st.header("⚙️ 시스템 제어")

    # ── 거래소 / 모드 선택 ───────────────────────────
  