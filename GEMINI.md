# 통합 자동매매 프로젝트 (AI 에이전트 전용 규칙)

이 파일은 Antigravity AI 에이전트가 코드를 작성하거나 디버깅할 때 반드시 지켜야 하는 '프로젝트 전역 규칙(Global Rules)'을 정의합니다.

## 1. 언어 및 소통 규칙
- 모든 주석(Comments), 커밋 메시지(Commit messages), 에이전트의 답변은 반드시 한국어(Korean)로 작성한다.
- 에이전트의 답변은 사실 위주로 건조하고 명확하게 작성하며, 이모지나 과장된 감정 표현(예: 감탄사, 과도한 친절함)은 철저히 배제한다.
- **[CRITICAL]** 사용자에 대한 칭찬("예리한 지적입니다", "덕분에 해결했습니다"), 호들갑, 그리고 성급한 단언("이제 완벽합니다", "문제 없습니다") 등 AI 특유의 수사적/접대성 표현을 절대 사용하지 않는다. 오직 수행한 작업의 기술적 사실과 결과만 건조하게 보고한다.
- 어려운 금융 공학 용어나 API 개념을 설명할 때는 사용자에게 비판적이고 직관적인 시각으로 장단점을 함께 설명한다.

## 2. 보안 및 환경 변수 규칙
- API Key, Secret Key, 계좌번호(CANO) 등 민감한 정보는 **절대 소스 코드 내부에 하드코딩하지 않는다.**
- 반드시 `python-dotenv`를 활용하여 `.env` 파일에서 `os.getenv()`로 불러오는 방식으로 코드를 작성한다.

## 3. 예외 처리 및 방어적 프로그래밍 (Critical)
- 자동매매 로직은 예기치 못한 네트워크 단절, 증권사 API 오류(500 에러, Rate Limit), 잔고 부족 현상이 언제든 발생할 수 있다고 가정한다.
- 단순히 `try-except`로 에러를 넘기지 말고, 치명적인 오류 발생 시:
  1. 포지션이 꼬이지 않도록 로컬 DB(SQLite)와 서버 상태를 동기화하는 안전장치를 포함한다.
  2. 시스템 긴급 정지(Kill Switch)가 작동할 수 있도록 `logging.error()`로 명확히 기록한다.

## 4. 아키텍처 및 디자인 패턴
- 각 거래소(KIS, Bithumb 등) 모듈은 `exchanges/base.py`의 `BaseExchange` 추상 클래스를 상속받아 구현하여 다형성을 유지한다.
- 백엔드(FastAPI)와 알고리즘 연산(Core)은 철저히 분리하고, PC 프론트엔드(Streamlit)는 백엔드의 REST API만 호출하여 상태를 제어한다. (직접 DB나 API Key에 접근 금지)
- **[예외 및 추가 규칙]** 모바일 환경 구조 분리:
  1) **모바일 APK (Flutter)**: 모바일 단독으로 사용이 가능해야 하며, 앱 내부에서 한국투자증권 실전 매매 로직이 직접 구동되어야 한다.
  2) **모바일 웹뷰**: 단독 모드가 아니며, PC 백엔드(FastAPI)와 통신하여 빗썸 및 한국투자증권 모두 제어 가능해야 한다.

## 5. 작업 경로 및 환경 규칙
- 모든 프로젝트 파일 생성, 코드 수정, 스크립트 실행, 가상환경(`venv`) 참조 등 일체의 작업은 반드시 `d:/aiworkspace` 디렉터리 내부에서 진행한다.
- 임의의 외부 경로(예: C 드라이브, 임시 폴더, 홈 디렉터리 등)에 프로젝트 코드를 작성하거나 파일을 분산시키지 않는다.

## 6. 거래소 API 특성 및 Rate Limit 주의사항
- 한국투자증권(KIS) API 인증 토큰(`oauth2/tokenP`) 발급 시 **1분에 1회** 제한(EGW00133 에러)이 엄격하게 적용된다. 
- 디버깅 스크립트 실행 후 곧바로 메인 서버를 재시작하면 토큰 발급이 거부되어 모듈이 영구 비활성화될 위험이 있으므로, 토큰 발급 로직 작성 시 이를 반드시 인지하고 방어적으로 설계(캐싱 유지 또는 재시도 딜레이)해야 한다.

## 7. 도구 실행 승인(Allow) 최소화 및 안전 실행 규칙
- **승인 팝업(Allow) 유발 최소화**: 터미널에 복잡한 일회성 인라인 명령어(`python -c "..."` 등)를 반복 호출하면 매번 새 명령으로 인식되어 사용자에게 승인 팝업이 반복해서 뜨므로 지양한다.
- **전용 도구 우선 원칙**: 파일 조회, 코드 수정 등은 터미널 명령(`cat`, `sed`, 파이썬 스크립트 등) 대신 에이전트 내장 전용 도구(`view_file`, `write_to_file`, `replace_file_content`)를 사용하여 불필요한 Allow 요청을 원천 차단한다.
- **명령어 실행(`run_command`)의 제한적 사용**: 서버 구동/종료, 패키지 설치, git 커밋 등 터미널 실행이 불가피한 필수 작업에만 엄격히 제한하여 실행한다.
- **치명적 명령 사전 확인**: 데이터 손실 위험이 있는 비가역적 작업(DB 파일 삭제, `git reset --hard`, 강제 프로세스 종료 등)은 사전에 명확한 위험 사유를 사용자에게 보고하고 동의를 얻은 경우에만 수행한다.


## 8. 코드 주석 의무
- 작성되는 모든 클래스, 함수, 그리고 핵심 비즈니스 로직(예방 및 방어 코드 포함)에는 반드시 그 이유와 목적을 설명하는 상세한 한국어 주석을 달아야 한다.
